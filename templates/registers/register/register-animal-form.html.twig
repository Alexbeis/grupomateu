{% block body %}
<form action="{{ path('index_register') }}" method="POST">
    <div class="form-group">
        <label for="scanner">Escanee:</label>
        <input name="entrance_path" type="text" class="form-control" id="scanner">
    </div>
    <button type="submit" class="btn btn-default pull-right">Añadir</button>
</form>

{% endblock %}