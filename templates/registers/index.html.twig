{% extends 'base.html.twig' %}

{% block body %}
    <section class="content-header">
        <h1>Registros de Entrada <small>Listado de Registros</small></h1>
        <ol class="breadcrumb">
            <li><a href="{{ path('index_dashboard') }}"><i class="fa fa-dashboard"></i> Dashboard</a></li>
            <li class="active">Registros</li>
        </ol>
    </section>
    <section class="content">
        <div class="row">
            <div class="col-xs-12 ">
                <div class="box">
                    <div class="box-header">
                        <div class="row">
                            <div class="col-xs-6">
                                <h4>Tabla de Registros</h4>
                            </div>
                            <div class="col-xs-6">
                                <div class="pull-right">
                                    <button type="button"
                                            class="btn btn-primary"
                                            data-toggle="modal"
                                            data-target="#modal-add-inc-register"
                                            data-url="{{ path("add_explotation") }}">
                                        Nuevo
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="box-body">
                    {% include 'registers/register/registers-table.html.twig' %}
                    </div>
                </div>
            </div>
        </div>
        {# # MODALS # #}
        {% include 'registers/modal/add-register.html.twig' %}
    </section>

{% endblock body %}