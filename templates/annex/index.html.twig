{% extends 'base.html.twig' %}

{% block body %}
    <section class="content-header">
        <h1>Marcados <small>Listado</small></h1>
        <ol class="breadcrumb">
            <li><a href="{{ path('index_dashboard') }}"><i class="fa fa-dashboard"></i> Dashboard</a></li>
            <li class="active">Marcados</li>
        </ol>
    </section>

    <section class="content">
        <div class="row">
            <div class="col-xs-12">
                <div class="box box-warning">
                    <div class="box-header with-border">
                        <div class="row">
                            <div class="col-xs-12 col-md-8">
                                <h3 class="box-title">Marcados</h3>
                            </div>
                            <div class="col-xs-12 col-md-4">
                                <div class="btn-group-horizontal pull-right">
                                    <a href="{{ path('export_annexed') }}.csv" type="button" class="btn btn-primary js-export-csv">Exportar csv</a>
                                    <a href="{{ path('export_annexed') }}.pdf" type="button" class="btn btn-primary js-export-pdf">Exportar Pdf</a>
                                </div>
                            </div>

                        </div>
                    </div>
                    <div class="box-body">
                        {% include 'annex/annex-table.html.twig' %}
                    </div>
                </div>
            </div>

        </div>
    </section>
{% endblock %}