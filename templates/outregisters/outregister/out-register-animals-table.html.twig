<div class="table-responsive">
    <table id="out-register-animal-table" class="table table-striped table-bordered" style="width:100%">
        <thead>
        <tr>
            <th>Crotal</th>
            <th>Número interno</th>
            <th>Crotal Madre</th>
            <th>Edad</th>
            <th>Edad(meses)</th>
            <th>Acciones</th>
        </tr>
        </thead>
        <tbody>
        {% for animal in outregister.animals %}
            <tr>
                <td>{{ animal.crotal }}</td>
                <td>{{ animal.internalnum }}</td>
                <td>{{ animal.crotalMother }}</td>
                <td>{{ animal.birthdate|date('d-m-Y') }}</td>
                <td>{{ animal.birthdate|calculateMonths }}</td>
                <td>
                    <a class="btn btn-default" href="{{ path('edit_animal', {'id':animal.id}) }}#sheet">Ver/editar</a>
                    <a class="btn btn-default js-remove-inc-register-animal"
                       href="{{ path('register_delete_crotal')}}"
                       data-id="{{ animal.id }}"
                       data-increg="{{ outregister.id }}"
                    >
                        <i class="fa fa-trash"></i>
                        <span class="js-spinner">
                            <i class="fa fa-spinner fa-spin hidden"></i>
                        </span>
                    </a>

                </td>
            </tr>
        {% endfor %}
        </tbody>
        <tfoot>
        <tr>
            <th>Crotal</th>
            <th>Número interno</th>
            <th>Crotal Madre</th>
            <th>Edad</th>
            <th>Edad(meses)</th>
            <th>Acciones</th>
        </tr>
        </tfoot>
    </table>
</div>