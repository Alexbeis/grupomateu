<!doctype html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link href="https://fonts.googleapis.com/css?family=Open+Sans:400,400i,700,700i" rel="stylesheet">

    <style>
        table {
            font-family: arial, sans-serif;
            border-collapse: collapse;
            width: 100%;
        }

        td, th {
            border: 1px solid #dddddd;
            text-align: left;
            padding: 8px;
        }

        tr:nth-child(even) {
            background-color: #dddddd;
        }
    </style>
    <title>Listado Anexos</title>
</head>
<body>
<div class="container main-container">
    {% block body %}
        {% set first = anexed|first  %}
        {% set owner = first.animal.explotation.owner %}
        <h2>Hoja de Identificación Individual</h2>
        <div class="container">
            <p>
                Impresión generada por: {{ app.user.email }}
            </p>
            <p>
                Fecha: {{ "now"|date("m/d/Y") }}
            </p>
            <p>
                Nombre, apellidos y Dni: {{ owner.name }} <span style="background-color: yellow">{{ owner.nif }}</span>
            </p>
            <p>
                {# TODO: Code should be passed as a variable#}
                Código de la Explotación: <span style="background-color: red" >{{ first.animal.explotation.code }} </span>
            </p>

            <p>DECLARA que los {{ anexed|length }} animales amparados con la G.O.S.P. serie y número ..................
            corresponden a los crotales, certificados de traslado y origen siguientes: </p>
        </div>
        <table>
            <tr>
                <th></th>
                <th>Crotal</th>
                <th>Numero Interno</th>
                <th>Fecha Nacimiento</th>
            </tr>
            {% for anex in anexed %}
                <tr>
                    <th>{{ loop.index }}</th>
                    <th>{{ anex.animal.crotal  }}</th>
                    <th>{{ anex.animal.internalNum }}</th>
                    <th>{{ anex.animal.birthDate|date('d-m-y') }}</th>
                </tr>
            {% endfor %}
        </table>

        <div class="divider" style="margin-top: 20px;">
            <div class="row">
                <div style="text-align: left;" >
                    Benabarre, a ......... de ........... de ...........
                </div>
                <div style="text-align: right;">
                    Firma del titular o representante
                </div>
            </div>
        </div>

    {% endblock %}
</div>
{% block javascripts %}
    <script src="{{pdf_url}}build/manifest.json"></script>

{% endblock %}
</body>
</html>